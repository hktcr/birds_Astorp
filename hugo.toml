baseURL = 'https://astorpsfaglar.se/'
languageCode = 'sv'
title = 'Fågelåret i Åstorp'

publishDir = 'docs'

# Permalinks
[permalinks]
  posts = '/posts/:year/:month/:slug/'

# Taxonomies
[taxonomies]
  tag = 'tags'
  month = 'months'
  species = 'species'

# Menu
[menus]
  [[menus.main]]
    name = 'Hem'
    pageRef = '/'
    weight = 10
  [[menus.main]]
    name = 'Notiser'
    pageRef = '/posts/'
    weight = 15
  [[menus.main]]
    name = 'Årshjul'
    pageRef = '/artguide/'
    weight = 25
#  [[menus.main]]
#    name = 'Fenologi'
#    pageRef = '/fenologi/'
#    weight = 26
  [[menus.main]]
    name = 'Efterlysta'
    pageRef = '/efterlysta/'
    weight = 26
  [[menus.main]]
    name = 'Horisonten'
    pageRef = '/vader/'
    weight = 27
  [[menus.main]]
    name = 'Galleri'
    pageRef = '/galleri/'
    weight = 28
  [[menus.main]]
    name = 'Aktuella observationer'
    pageRef = '/faltpulsen/'
    weight = 29
  [[menus.main]]
    name = 'Krysskarta'
    pageRef = '/karta/'
    weight = 20
  [[menus.main]]
    name = 'Om'
    pageRef = '/om/'
    weight = 40


# Params
[params]
  description = 'En fågelårsbok för Åstorps kommun — observationer, bilder och krysslista'
  author = 'Håkan Karlsson'
  municipality = 'Åstorp'
  year = 2026

# Build settings
[build]
  writeStats = true

# Date format
[frontmatter]
  date = ['date', 'publishDate', 'lastmod']
  lastmod = ['lastmod', ':git', 'date', 'publishDate']
  publishDate = ['publishDate', 'date']

# Hugo server settings
[server]
  [[server.headers]]
    for = '/**'
    [server.headers.values]
      X-Frame-Options = 'DENY'
      X-Content-Type-Options = 'nosniff'

# Markup settings - allow raw HTML in markdown
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
